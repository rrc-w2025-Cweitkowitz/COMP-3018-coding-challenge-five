import { Resources } from "../models/resourceModels";





export const getAllResources = async(): Promise<Resources[]> => {
    try {
        const resourcesReturn =
    } catch (error) {
        throw new Error(
            "Cannot find items"
        )
    }
}